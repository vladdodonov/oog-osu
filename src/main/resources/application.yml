server:
  port: 8895
  session:
    timeout: 259200s

spring:
  jpa:
    properties:
      hibernate:
        show_sql: false
        format_sql: false
        use_sql_comments: false
        generate_statistics: false
  datasource:
    driverClassName: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:postgresql://localhost:5432/oog-osu
    username: oog-osu
    password: oog-osu

swagger:
  enabled: true

flyway:
  enabled: true

emailservice:
  smtp:
    smtp-server-name: "smtp.gmail.com"
    smtp-server-port: 587
    user-name: "og.osu.dodonov@gmail.com"
    user-password: "Qwerty123!"